#ifndef MYSTACK_H#define MYSTACK_H#include "Coordinate.h"/*****************************栈：先进后出*****************************/class MyStack{public:	MyStack(int size);	~MyStack();	bool stackEmpty();	bool stackFull();	void clearStack();	int stackLength();	bool push(Coordinate elem);		//入栈	bool pop(Coordinate &elem);		//出栈	void stackTraverse(bool);	//遍历栈private:	Coordinate *m_pBuffer;			//栈空间指针	int m_iSize;				//栈容量	int m_iTop;					//栈顶、栈中元素个数};#endif